from print_tricks import pt

from mod_category_z_trajectory import *




if __name__ == "__main__":
    ## NOTE: The class instantiation in the tests below is just for testing purposes. These classes 
    ## should never be instantiated, but are just used as namespaces to find the methods I need to 
    ## dynamically add to the live combility instances. 
    Path_Shape().path_shape()
    Path_Range().path_range()
    Path_Curve().path_curve()
    Target_Types().target_types()
